# Glyph Factory · 用户意图登记表

这份文件记**用户要什么**，原话照抄，不改写。

`aha.md` 记的是 Aha / 披露契约——**怎么实现**。这份记的是需求本身——**要实现成什么样**。
两份文件各管一头，**不再互为副本**：加产品需求改这里，改实现契约改 `aha.md`。
（在 2026-09-21 之前这两份是同一份文件的逐字节副本，那是历史遗留，已拆开。）

每条意图四栏：**原话**（用户自己怎么说的）· **操作化**（落在哪张表、哪个数上）·
**怎么验**（命令与证据路径）· **状态**。

---

## U1 · 相邻两条 Aha 之间的点击要低标准差、要好均值

> fix the clicks between different aha moments until we reach to a low std. and GOOD mean version .
> this is the baseline for yourrefrence .
> —— 2026-09-21

**操作化。** 节奏按「相邻两条 Aha 之间」读，不按幕读：幕是作者的划分，Aha 才是玩家收到的
节拍。三个单位各回答一个问题——秒（等了多久）· 决策点击（这一段有几个真判断）· 全部点击
（这一段按了多少下）。参考玩家只按屏幕上写着的东西走（`tests/pacing.mjs`），不自己拍预算，
否则量到的是机器人不是游戏。

**怎么验。** `node scripts/pacing.mjs` 六个口径全打；断言在 `tests/game-v3.test.mjs` 的
「a plain playthrough reaches the ending, and the rhythm between Aha moments holds」；
基线 `tests/pacing-baseline.json`，改门槛后用 `--diff` 对照。判据与三种坏形状见
`.claude/skills/verify/SKILL.md` 的 rhythm contract。

**判据（2026-09-21 由 U7 改写，理由见那一节的「连带」）。** 原来是 `时长 CV ≤ 0.25`，
那是「塌缩」的代理指标；把它压到极限会得到 27 个一模一样的房间，而「每个房间都是中等强度」
本身就是节奏缺陷。改成守「有形状、但形状有界」：

> 时长 CV ≤ 0.45，且 最长 ÷ 最短 ≤ 6，且 没有一段短于 60 秒，且全程 ≥ 7200 秒。

塌缩仍然拦得住（塌缩是 CV 更大、且最短段趋近 0），设计出来的快慢不会被误杀。

**状态。** 已达成。新判据下的读数：时长 CV 0.403（均值 270.0s / 标准差 108.9s / 90–490s）、
最长÷最短 5.43、全程 121.7 分钟；去掉第一章教学后决策 4.60 / 3.59、点击 4.68 / 3.60；
0 逆序、0 同拍、1 处静默拍（288s）。改判据前的那组是 CV 0.069 / 272.8±18.7s——
把 CV 从 0.069 抬到 0.403 是这一轮的设计结果，不是回归。
**「全部 27 段点击」这一口径刻意不进判定**——它量的是教程长度不是节奏，理由见 U3。

---

## U2 · 「均值好」＝至少能玩两小时

> when i say mean is good. i mean it will offer at least 2 hours time playing it .
> —— 2026-09-21

**操作化。** 读**全程秒数**，不读任何一段的平均。27 条发现各有一条会自己长的钟
（`AHA_CLOCK`：第二章读者、第四章夜间文章、第五章机器用量、第六章歧义），相邻刻度之差
按 ~280 秒写，合起来两小时。

**怎么验。** `node scripts/pacing.mjs` 报「全程 X 分钟（≥2 小时 ✓）」；断言
`totalSeconds >= 7200`。

**状态。** 已达成：121.7 分钟（7302 秒）——读的是**被动**那条路（屏幕写着什么就做什么）。
主动玩更短（97.6 分钟），那是设计意图，不是这条需求被打破。
已知边界：离线也累积（上限 8 小时），挂机回来的玩家会跳过一部分等待——这是放置游戏的
正常行为，不是契约漏洞；「两小时」说的是从头连续玩。

---

## U3 · 每一章都要有第一章那种可重复的手部动作

> give each new aha momement the repatable movements just like the fist aha moment. we liek this really !
> —— 2026-09-21

**操作化。** 第一章的手感是「随时有一下可按，按了世界就动」——参考对局里它每秒 2 下。
第二到六章现在没有这个：四个钟只自己涨，点击对它们的贡献最多 11%（`composed` 封顶 12 之后
只值 0.18/秒），所以按不按都是四分半，点击在第二到六章是装饰性的。
要做的是**每章一个可重复动作，直接往那一章的钟里灌**，代价随按次递增，保证被动玩仍 ≥2 小时。
设计参照见 U6。

**怎么验。** 四层，缺一层就不算：
1. **被动路径不变**：节奏断言（`npm run verify:fast`）仍全绿，全程仍 ≥7200 秒；
2. **主动路径真的更快**：参考玩家在每章用它，读数里能看到那几段秒数下降；
3. **真实可点**：浏览器契约里每章有一个可见可点的按钮，点了钟真的涨（不是只改文案）；
4. **负对照**：把递增代价去掉，2 小时地板必须被打破——否则说明这个动作没有约束力，
   「点击换时间」是假的。

**状态。** 已达成（2026-09-21，形式由 U7 定稿）。第二到六章各有一个可重复动词，按下去本幕的钟
永久加速、代价递增；被动路径仍是 121.7 分钟（地板在），主动路径 97.6 分钟。
四层验证全在 `tests/rhythm-structure.test.mjs`：被动断言、主动更快、真实可点（引擎的
`commandReady` 是渲染层的同一份真源）、以及「拆掉倍率主动路径退回被动长度」这条负对照。
中途一轮子任务把这条记成「用户改选：不加手部动作、改成计划型」，随后用户当面推翻；
记录打架的过程与被否掉的候选留在 `prototypes/pacing-options.answer.md`——
`/verify` 只写方法、不写决定，所以存档不放那里。

---

## U4 · 节奏要能被 /verify

> after job is done. add how u /verify this 游戏节奏 in good way
> —— 2026-09-21

**操作化。** 不是写一段说明，是把读数变成可复现的东西：参考玩家、报表、基线文件、
进快门的断言、以及「改完怎么对照」的流程。

**怎么验。** `.claude/skills/verify/SKILL.md` 的 rhythm contract 一节；
基线 `tests/pacing-baseline.json`；`node scripts/pacing.mjs --diff`；
可视化 `node scripts/eli5-pacing.mjs` → `public/eli5-pacing.html`。

**状态。** 已达成。

---

## U5 · 用户意图本身要单独存档、单独可验

> add how we verify user intent just like above in a new part in /verify the intent part please .
> save user intent with intent.md and intent.html
> —— 2026-09-21

**操作化。** 这份文件（`intent.md`）是用户意图的唯一真源，`public/intent.html` 是它的
可读版本；两者内容一致，但**都不是 `aha.md` 的副本**。`/verify` 里要有对应的一节，
说明每条意图怎么验、证据在哪。

**怎么验。** `tests/intent-audit.test.mjs` 断言两条：意图文档不再与 `aha.md` 逐字节相同，
且每条 U 编号在 `.md` 与 `.html` 里都在、都带「原话」与「怎么验」两栏。

**状态。** 已达成（本次）。

---

## U6 · 手感参照 Universal Paperclips

> so what does paperclip solve this ?
> —— 2026-09-21

**结论（来源见下）。** UP 不是靠「让人一直点」解决等待的——它中段同样有死时间，评测里
明说「第二小时里有一段在等 trust 升级，活跃玩法慢下来，有些玩家在这里流失」。
它解决的是**别让屏幕上只剩一个计时器**，靠三件事：

1. **每一阶段换一种基础资源和一种手部动词。** 第一阶段按「做回形针」，之后是定价/营销、
   分配无人机（Work/Think 滑杆）、配置探测器 Trust。手部动作没有消失，是**换了皮**。
2. **那个动词永远对着当前的瓶颈。** 玩家做的都是「一次小修补」：收入慢就调价、产量慢就买
   设备、设备贵就多做、多做要线材……瓶颈一个接一个被造出来。原文：系统「几乎不给你留反思
   的余地，因为它一直在生成新的活」。
3. **原来的按钮不删只降级。** 「Make Paperclip」按钮整局都在，到最后灰掉；结局还要你亲手
   按 120 下拆掉自己——手部动作的消失本身就是叙事。

**对字工厂的意思。** 我们已经有 UP 那种「每阶段一种新资源」（读者 / 文章 / 机器用量 / 歧义），
缺的正是第 1、2 条：**没有一个动词对着当阶段的瓶颈**。U3 就是补这个。

**怎么验。** 这条不是功能，是设计依据，验法是**可追溯**：U3 的每个动作都要能指回本节
三条之一，指不回去就说明是凭感觉加的。另外第 3 条（中段仍有死时间）是**反面参照**——
它说明「UP 也这样」不能当作我们四分钟空转的辩护，只能当作「别让屏幕上只剩计时器」的理由。

**状态。** 研究已完成，结论落在这里；实现见 U3。

---

## U7 · 这一轮的方向：快慢有结构 + 复利推钟 + 微事件

> 「均质」→ **改成有设计的快慢**
> 「点击不累积」→ **复利：每按一次，这一章以后每一段永久快 5%，每章上限 +50%**
> 「等待期要不要加东西」→ **加微事件**
> —— 2026-09-21 问卷选项（用户点选的选项，不是手打原话）

**背景。** 先做了一轮竞品批评自查（见下节），再把候选解法摆给用户拍板。
四个问题里「旧动词死掉」那条的答案是「换意义（Orchard Deck 式）：旧动作的产出变成新系统的燃料，
第一章印的字成为第五章机器的训练材料」。

**操作化（已实现，读数见下）。**

1. **快慢有结构**：每一幕的段长按「首段 ×0.45（喘口气）、末段 ×1.7（攒劲）、其余 ×1.0」重排，
   **按幕归一化**——每一幕的总秒数不变。实现方式是把四条阶梯的**刻度**重排（秒数 = 刻度差 ÷
   增速，所以只能解一个不动点，不能手调），脚本是 `scripts/fit-rhythm.mjs`。
   实测标准差 18.7 → 108.9s，最长÷最短 4.54 → **5.43×**。
2. **复利推钟**：每章一个可重复动词（第二章「发行新一期」· 第三章「张贴告示」·
   第四章「让 Agent 加班」· 第五章「喂机器一批文本」· 第六章「投产一条新语法」）。
   每按一次本幕时钟永久 +5%，每章上限 +50%（10 下到顶），代价随按次递增。第一章没有。
   加成只乘在本幕自己的钟上：第二章按的几下不会让第三章的读者也变快。
3. **微事件**：等待中按「本幕待了多久」确定性地出现一个可点的世界内东西（第二章「一封没署名的
   信」· 第三章「墙上被人抄走的一句话」· 第四章「一份没人署名的退稿」· 第五章「一段对不上的话」·
   第六章「一句没有人认领的句子」）。**它和主循环的机制差别是「减法」**——主循环只往钟里加，
   微事件花掉一样别的东西（库存字 / 意义）换本幕的钟往前走 45 秒的自然增量，有的还留下噪音。
   这是对 AdCap 事件疲劳那条的正面回答：不是「同一套机制换张画」。
4. **旧动作供新层**：第五章的钟被**旧层产能**喂着（`machineUseRate` 里那一项 `rate(g)/10`），
   第五章的动词也花库存字。这一条原来只是文案：参考对局里产能从第二章到结局一动不动
   （134/秒），玩家再也没碰过第一幕买的机器。接上之后产能一路涨到 265/秒，一局买机器
   42 → 77 台。

**怎么验。** 每一条自带负对照，缺一条就不算做到。全部落在
`tests/rhythm-structure.test.mjs`，每条负对照都真的改引擎源码再跑一遍：

- 快慢结构：把曲线拉平（每一幕的目标秒数取该幕均值，用同一个 `reshape` 重排），
  U1 的四条判据必须**仍然全绿**，而「首段最短、末段最长」必须失败——两条都验，
  才说明形状是设计出来的、而判据守的是「有界」不是「有形状」。
- 复利推钟：主动玩家（按动词 + 捡事件）必须比被动玩家短一截（实测 121.7 → 97.6 分钟）；
  再把倍率那一项拆掉（动词还在、代价还照付），主动路径必须退回被动路径的长度。
- 微事件：秒数没走够时不可点，走够了才可点，点掉之后要再等；点一次推进的量恰好是
  「增速 × 45 秒」；被动路径上一个都不捡。
- 旧动作供新层：把产能那一项拆掉，第五章的钟就不再随旧摊子大小变化。

另有一条不动的地板：**被动玩仍 ≥ 7200 秒**（实测 7302 秒 / 121.7 分钟）。
主动玩更短是设计意图，不是漏洞。

**被否掉的候选**（连同否掉的理由一起记，免得下次重新发现一遍）：

| 候选 | 否掉的理由 |
| --- | --- |
| 每幕两条速度不同的钟并行 | 与「复利推钟」解决的是同一件事（让等待期有可权衡的东西），同时做改动面翻倍且不好归因 |
| 一次性充值式推钟 | 按完当前这段就回原速，玩家会算出「按它不如等」，按钮变成装饰 |
| 把点击改成纯计划型（不加手部动作） | 那样点击仍是装饰性的，U3 要的东西没被满足 |
| 限时倍率 | 和微事件重叠，但更弱：它只改数字，不引入世界里的东西 |

**状态。** 已达成（2026-09-21）。四条各自的负对照都在 `tests/rhythm-structure.test.mjs` 里，
改完 `npm run verify:fast` 全绿（97 项）。原型与结论存档：`prototypes/pacing-options.html`
（可开可点）、`prototypes/pacing-options.answer.md`；拟合脚本 `scripts/fit-rhythm.mjs`
（`--dry` 只打印不写回）。

**连带：U1 的判据要改（本次已改完，U1 那一节是真源）。** 新结构下时长 CV = 109.1 ÷ 273 = **0.40**，
而 U1 当时写的是 `CV ≤ 0.25`，会直接判这条失败。改成：

> 时长 CV ≤ 0.45，且 最长 ÷ 最短 ≤ 6，且 没有一段短于 60 秒，且全程 ≥ 7200 秒。

理由：原判据是「塌缩」的代理指标（2026-09 的真问题是后五章全挤在 35 下里演完），
但把它压到极限会得到 27 个一模一样的房间——而「每个房间都是中等强度」本身就是节奏缺陷。
新判据守的是「有形状、但形状有界」：塌缩仍然拦得住（塌缩是 CV 更大且最短段趋近 0），
设计出来的快慢不会被误杀。

## 竞品批评自查（2026-09-21）

只看**能追溯到具体游戏和具体毛病**的批评；泛泛的「放置游戏很无聊」不进表。
优先设计师自述、开发者复盘、同行评审论文；纯聚合站只当线索不当依据。

| 类型通病 | 谁被点名 | 反例 / 解法 | 字工厂自查 |
| --- | --- | --- | --- |
| 自动化抽走能动性 | 全部纯点击类；Alharthi CHI'18 承认重复机制可能诱发点击疲劳 | 每阶段换一种手部动词（Universal Paperclips） | **有** → U7 的复利推钟 |
| 均质无纹理 | Clicker Heroes；Pixel Poppers：「a treadmill… the only way to win is not to play」 | 有设计的快慢；阈值倍率 | **有** → U7 的快慢结构 |
| 旧动词死掉 | AdVenture Capitalist 及多数同类：新层一出旧生成器就没意义 | AdCap 的 Newspapers 加成其他投资；Derivative Clicker 用「买过的数量」给同层永久加成 | **有** → U7 的换意义 |
| 中后期墙 + 逼氪 | AdVenture Capitalist：后期「days or weeks between significant upgrades」 | 转生；离线收益设上限保护节奏 | **无**（免费、有结局、离线 8 小时上限） |
| 事件疲劳 | AdCap：「mechanics are identical to the main game with different art」 | —— | **无**，但 U7 要加事件 → 这条从「别做」变成**验收条件** |
| 没有结局 → 流失 | 多数无结局放置游戏 | Universal Paperclips 的四小时作者式结局 | **无**（A28 停止印刷是真结局） |
| 231 小时却毫无喜爱 | Clicker Heroes；Eurogamer：「symptoms of loving a game, without ever feeling a scrap of fondness」 | 数字之外要有可回收的东西 | **部分**：日志/世界线给了叙事回收，但玩家的操作还没被回收进叙事 |
| 加法没有减法 = 等待不是决定 | Orchard Deck：「addition with no subtraction is a wait, not a decision」 | 让产出可消耗、可转化 | **有** → U7 的换意义 |

**「不在其中」和「在其中」一样重要**：它同样是一条要守住的性质，所以每一行都有自己的负对照。
这张表是**证据与决定**，住在需求文档里；`/verify` 里只留方法，不抄结论。

## 可执行验收

| ID | 必须发生的事实 | 验证方式 |
| --- | --- | --- |
| U1 | 相邻两条 Aha 之间的秒数有形状但不失控（CV ≤ 0.45、最长÷最短 ≤ 6、无一段 < 60s），点击数稳定，顺序不乱、不挤在同一拍 | `node scripts/pacing.mjs` 六口径 + `tests/game-v3.test.mjs` 节奏断言 + `--diff tests/pacing-baseline.json` |
| U2 | 从头连续玩，全程不少于两小时 | `pacingReport().totalSeconds >= 7200`，报表里那行「≥2 小时 ✓」 |
| U3 | 第二到六章各有一个可重复动作，按了直接推进当阶段的钟；被动路径仍满足 U1/U2 | `tests/rhythm-structure.test.mjs` 的「复利推钟」一条：主动更快 + 拆掉倍率必须退回被动长度；按钮可点由浏览器契约（`tests/browser/test_player.py`）覆盖 |
| U4 | 节奏读数可复现、可对照、可回归 | `.claude/skills/verify/SKILL.md` rhythm contract；基线文件；`--diff` |
| U5 | 用户意图单独存档，且与 Aha 契约不再互为副本 | `tests/intent-audit.test.mjs` 的非同一性与完整性断言 |
| U6 | 设计上有据可依（不是凭感觉） | 本节列出的来源；U3 的实现要能对上三条之一 |
| U7 | 每一幕的等待有设计出来的快慢；每章有一个按下去会永久加速本幕时钟的动词；等待中有世界内微事件；旧动作的产出供新层 | `tests/rhythm-structure.test.mjs` 五条（形状 + 四条负对照，每条都真的改源码再跑）+ 被动路径仍 ≥7200 秒 |

## 来源

- U6 结论来自对 Universal Paperclips 的公开分析：Oli Z. 的逐阶段拆解
  （<https://oliz.io/blog/2022/game-analysis-universal-paperclips/>）、
  Schmalzer 在 *Eludamos* 上的论文
  （<https://doi.org/10.7557/23.6174>）、以及一篇明确指出中段死时间的评测
  （<https://dinogame.gg/blog/universal-paperclips-review/>）。
  「每阶段一种新资源 + 中段仍有死时间」这两点三处互相印证。

## 非目标

- 不把 `intent.md` 当作实现文档；实现的真源是引擎表（`AHA_CLOCK` / `ACT_GATES` / `COMMAND_COSTS`）。
- 不为了满足某条意图去改判定口径。改口径要先改这份文件，并写清为什么。
