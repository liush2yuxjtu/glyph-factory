12×12 像素精灵，14 个，只按整数倍放大。

- 调用：`GlyphPixel.sprite(name, size)`，size 为 24 / 36 / 48；`GlyphPixel.sprites` 列出全部名字。
- 着色走 CSS 变量（`ink` `panel` `sunk` `lamp` `accent` `danger`），随主题切换。需要文件时用 `assets/Icons/` 的 SVG。
